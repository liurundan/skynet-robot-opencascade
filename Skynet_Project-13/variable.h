#ifndef VARIABLE_H
#define VARIABLE_H

#include <vector>

extern bool start;
extern bool ready;

extern bool okmove;
extern unsigned int ammount;
extern unsigned int programammount;
extern bool mode_hal;
extern bool mode_feedback;
extern bool mode_ikfrominit;

class variable
{
public:
    variable();
};

#endif // VARIABLE_H
